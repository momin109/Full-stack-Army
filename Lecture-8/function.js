
function name_of_the_function(/* parameter*/) {
    //function body
    // you can write any valid js code here

    //return result
}
name_of_the_function(/*argument*/)

//function-> there are two steps
//- Define a function
//- Invoke a function


/* function textFunction() {
    const a = 10;
    const b = 20;
    const result = a + b + Math.max(a, b) // ai khane [a+b] zog kore total pailo 30. and + [a , b compare kore dekhlo ze max mane boro songkha nibe]
    const num = Math.max(a, b)
    console.log(num)
}

textFunction() */

// const golobal = 200 ai ta ke global variable bolte paren
/* function textFunction(a=10,b=20-> ai ta ke bola hoi defult value) {
    function body moddhe za nebo ta local variable . function moddhe take , baire access nete pare na
}

textFunction(baire teke zokhon kono variable pass korchis ai te bola hoi position argument) */



/* function textFunction(input za nebo special variable) {
    const result = a + b + Math.max(a, b) // ai khane [a+b] zog kore total pailo 30. and + [a , b compare kore dekhlo ze max mane boro songkha nibe]
    return result;
}
const sum = textFunction(10, 20)
console.log(sum) */